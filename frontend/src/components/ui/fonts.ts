import { Inter, Mulish, Oswald } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const mulish = Mulish({ subsets: ['latin'] });

export const oswald = Oswald({ subsets: ['latin'] });